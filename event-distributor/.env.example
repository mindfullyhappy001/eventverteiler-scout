# Vite Environment Variables (optional - app will work without these)
# VITE_API_URL=http://localhost:8080

# Supabase Configuration (automatically set in app)
# VITE_SUPABASE_URL=https://your-project.supabase.co
# VITE_SUPABASE_ANON_KEY=your-anon-key

# Note: The app automatically configures Supabase credentials
# and can switch between API mode and direct Supabase mode